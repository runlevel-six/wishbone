apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/name: wishd
  app.kubernetes.io/part-of: wishbone

resources:
  - storage.yaml
  - backup-script.yaml
  - deployment.yaml
  - service-ingress.yaml
  - networkpolicy.yaml
  # secret.example.yaml is documentation, not a resource. Supply the real
  # Secret out of band.

# Uncomment once the extraction sidecar image is built and pushed. See
# extractor-sidecar.yaml and docs/how-to/enable-link-lookup.md.
# patches:
#   - path: extractor-sidecar.yaml
